var baseUrl   = window.location.origin+'/arpiya/';
//var baseUrl   = window.location.origin+'/spaatech-demo-arpiya/';
function populateVendor(vendorArr){ alert(vendorArr);
  var vendorArr = JSON.parse(vendorArr);
  $('#name').val(vendorArr. name);
}